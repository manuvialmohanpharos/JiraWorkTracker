<Window x:Class="JiraWorkTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JiraWorkTracker"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Jira Work Tracker" Height="500" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="Auto"/> <!-- Inputs -->
            <RowDefinition Height="Auto"/> <!-- Button -->
            <RowDefinition Height="10"/>  <!-- Spacer -->
            <RowDefinition Height="*"/>   <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Jira Work Tracker" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <!-- Input Section -->
        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <!-- Jira Work Item -->
            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock Text="Jira Work Item" HorizontalAlignment="Left" Margin="0,0,0,2"/>
                <TextBox Width="180" Height="40" Margin="0,5,0,0" materialDesign:HintAssist.Hint="Enter Jira id..."/>
            </StackPanel>
            <!-- Original Est. -->
            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Original Est." HorizontalAlignment="Left" Margin="0,0,0,2"/>
                <TextBox Width="100" Height="40" Margin="0,5,0,0" materialDesign:HintAssist.Hint="In hrs"/>
            </StackPanel>
            <!-- Remaining Est. -->
            <StackPanel Grid.Column="2" Margin="10">
                <TextBlock Text="Remaining Est." HorizontalAlignment="Left" Margin="0,0,0,2"/>
                <TextBox Width="100" Height="40" Margin="0,5,0,0" materialDesign:HintAssist.Hint="In hrs"/>
            </StackPanel>
        </Grid>

        <!-- Start Working Button -->
        <Button Grid.Row="2" Content="Start Working" Width="180" Height="45" HorizontalAlignment="Center" Margin="0,20,0,0"/>

        <!-- DataGrid for Work Log -->
        <DataGrid Grid.Row="4" Margin="0,20,0,0" AutoGenerateColumns="False" CanUserAddRows="False" Height="220" RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Jira ID" Width="*"/>
                <DataGridTextColumn Header="Started Working at" Width="*"/>
                <DataGridTextColumn Header="Running Timer" Width="*"/>
                <DataGridTextColumn Header="Stopped Working at" Width="*"/>
                <DataGridTextColumn Header="Log hours" Width="*"/>
                <DataGridTemplateColumn Header="Log" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Log" Width="50" Height="32"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
